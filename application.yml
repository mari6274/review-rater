spring:
  main:
    web-environment: false
  datasource:
    url: jdbc:h2:file:./reviews.db
    username: h2
    password: h2
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
